const dialog=document.getElementById('bnaDialog');document.querySelectorAll('[data-open-funnel]').forEach((el)=>{el.addEventListener('click',()=>dialog.showModal())});const params=new URLSearchParams(window.location.search);['utm_source','utm_medium','utm_campaign','utm_content','utm_term'].forEach((key)=>{const value=params.get(key);if(value)window.localStorage.setItem('bna_'+key,value)});